
// Your implementation here.
(function( window, undefined) {

    'use strict';

    window.$$ = window.tQuery = function( selector ) {

        var tQuery = function() {

        };

        tQuery.forEach = function() {

        };

        tQuery.length = 0;

        return tQuery;

    };

}( window ));